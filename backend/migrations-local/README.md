# Local-only migrations

Demo seed data for development. Applied automatically by `deploy/docker-compose.yml` on stack startup (dev only — use a production compose without this service for real deploys).

Manual re-apply:

```powershell
.\scripts\seed-demo.ps1
```

Demo login: `admin@nowhere.com` / `password123`
