# Roadmap

## Phase 2 — Real-time updates

- Backend: `GET /v1/stream/summary` (SSE or WebSocket)
- Web + Android: subscribe on Dashboard focus; update totals without full reload

## Later

- iOS client (SwiftUI) against the same API
- OpenAPI spec + codegen for mobile DTOs
- Bank / CSV import
- Multi-currency and budgets
- Categories UI and default category seeds
- Refresh token rotation and secure storage hardening
- Production HTTPS and certificate pinning
